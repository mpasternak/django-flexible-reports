============
Installation
============

At the command line::

    $ easy_install django-flexible-reports

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-flexible-reports
    $ pip install django-flexible-reports
