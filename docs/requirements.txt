# Dependencies for building the documentation.
#
# Deliberately separate from pyproject.toml: the docs are prose only, they
# import nothing from this package, and nobody installing
# django-flexible-reports needs MkDocs.
#
#   mkdocs build --strict
mkdocs>=1.6
mkdocs-material>=9.5
pymdown-extensions>=10.7
